<?xml version="1.0" encoding="utf-8"?>
<!-- Exposes only the one cache folder a Recon Card is ever written to - see ReconCard.kt. -->
<paths>
    <cache-path name="recon_cards" path="recon_cards/" />
</paths>
