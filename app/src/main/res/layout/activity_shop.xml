<?xml version="1.0" encoding="utf-8"?>
<!--
  The Shop shell. Every section inside shopCategoryContainer is inflated at
  runtime from Shop.categories, so this file does not change when the catalogue
  grows - only Shop.kt and strings.xml do.

  The whole screen scrolls as one. The title, the subtitle and the way out used
  to sit outside the scroll region and stay put while the catalogue moved behind
  them, which on a shop this long left a fixed band of chrome eating the top and
  bottom of every screenful.

  The purse is the exception, and the only thing here still pinned. A price is
  meaningless without it, and scrolling away from your own balance to read one
  is the thing it was put on screen to prevent.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_app_gradient"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:fitsSystemWindows="true">

    <!-- Purse. Prices are meaningless without it, so it stays on screen. -->
    <TextView
        android:id="@+id/shopScrapText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/bg_pill_tan"
        android:elevation="6dp"
        android:outlineAmbientShadowColor="@color/shadow_warm"
        android:outlineSpotShadowColor="@color/shadow_warm"
        tools:targetApi="28"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:gravity="center"
        android:drawableStart="@drawable/ic_scrap"
        android:drawablePadding="6dp"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="@color/text_primary"
        android:text="@string/header_scrap_default"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="12dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingHorizontal="16dp"
        android:clipToPadding="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:clipChildren="false"
            android:clipToPadding="false">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/shop_title"
                android:textColor="@color/text_primary"
                android:textSize="26sp"
                android:textStyle="bold"
                android:textAlignment="center"
                android:layout_marginBottom="4dp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/shop_subtitle"
                android:textColor="@color/text_muted"
                android:textSize="13sp"
                android:textAlignment="center"
                android:layout_marginBottom="16dp" />

            <LinearLayout
                android:id="@+id/shopCategoryContainer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:clipChildren="false"
                android:clipToPadding="false"
                android:orientation="vertical" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/shopBackButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:layout_marginBottom="16dp"
                android:text="@string/btn_back_to_workshop"
                android:textAllCaps="false"
                android:textColor="@color/text_primary"
                app:backgroundTint="@color/card_white"
                app:strokeColor="@color/card_border"
                app:strokeWidth="1dp"
                app:cornerRadius="16dp"
                app:elevation="4dp" />

        </LinearLayout>
    </ScrollView>

</LinearLayout>
